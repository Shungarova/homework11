# Ввод двух списков чисел
n1 = list(map(int, input("Введите значения 1 списка: ").split()))
n2 = list(map(int, input("Введите значения второго списка: ").split()))
p = set()

# Подсчитываем числа, содержащиеся одновременно в обоих списках
newnumbers = set(n1) & set(n2)

count = len (newnumbers)
# Вывод результата
print("Количество чисел, содержащихся одновременно в обоих списках:", count)
